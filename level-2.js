// question 1

// I did not manage to solve the questin 6 so I did not manage to solve this either.

// question 1
function iAmAFunction(iAmAFunction) {
	console.log(iAmAFunction);
}

iAmAFunction("I am a function");

var innerFunction;

function outerfunction() {
	console.log();
}

outerfunction(innerFunction);
